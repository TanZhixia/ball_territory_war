#pragma once
#include <SFML/Window.hpp>
const int WINDOW_WIDTH = 1000;                              // 窗口大小
const int WINDOW_HEIGHT = 600;
const int WIDTH = 6000;                                     // 真实大小(输出视频大小)
const int HEIGHT = 3600;
const int WINDOW_STYLE = sf::Style::Close;                  // 窗口样式(只有标题栏和关闭按钮)
const int FPS = 60;
constexpr const char* VIDEO_NAME = "video.mp4";             // 视频名称
constexpr const char* WINDOW_TITLE = "Ball Territory War";  // 窗口标题
